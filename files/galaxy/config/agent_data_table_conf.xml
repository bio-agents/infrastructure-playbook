<!-- Use the file agent_data_table_conf.xml.oldlocstyle if you don't want to update your loc files as changed in revision 4550:535d276c92bc-->
<tables>
    <!-- TODO(hxr): test if we can remove path since specified in galaxy.yml -->
    <!-- Locations of all fasta files under genome directory -->
    <table name="all_fasta" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/all_fasta.loc" />
    </table>
    <!-- Locations of indexes in the BFAST mapper format -->
    <table name="bfast_indexes" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, formats, name, path</columns>
        <file path="/opt/galaxy/agent-data/bfast_indexes.loc" />
    </table>
    <!-- Locations of nucleotide BLAST databases -->
    <table name="blastdb" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/blastdb.loc" />
    </table>
    <!-- Locations of protein BLAST databases -->
    <table name="blastdb_p" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/blastdb_p.loc" />
    </table>
    <!-- Locations of protein domain BLAST databases -->
    <table name="blastdb_d" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/blastdb_d.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie mapper format -->
    <table name="bowtie_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bowtie_indices.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie2 mapper format -->
    <table name="bowtie2_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bowtie2_indices.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie color-space mapper format -->
    <table name="bowtie_indexes_color" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bowtie_indices_color.loc" />
    </table>
    <!-- Locations of indexes in the BWA mapper format -->
    <table name="bwa_indexes" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bwa_index.loc" />
    </table>
    <!-- Locations of indexes in the BWA color-space mapper format -->
    <table name="bwa_indexes_color" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bwa_index_color.loc" />
    </table>
    <!-- Locations of MAF files that have been indexed with bx-python -->
    <table name="indexed_maf_files">
        <columns>name, value, dbkey, species</columns>
        <file path="/opt/galaxy/agent-data/maf_index.loc" />
    </table>
    <!-- Locations of fasta files appropriate for NGS simulation -->
    <table name="ngs_sim_fasta" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/ngs_sim_fasta.loc" />
    </table>
    <!-- Locations of 2bit sequence files for use in Lastz -->
    <table name="lastz_seqs" comment_char="#">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/lastz_seqs.loc" />
    </table>


    <!-- Locations of 2bit sequence files for use in deepAgents -->
    <table name="deepAgents_seqs" comment_char="#">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/deepAgents_seqs.loc" />
    </table>


    <!-- Locations of PerM base index files -->
    <table name="perm_base_indexes" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/perm_base_index.loc" />
    </table>
    <!-- Locations of PerM color-space index files -->
    <table name="perm_color_indexes" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/perm_color_index.loc" />
    </table>
    <!-- Location of SAMAgents indexes and other files -->
    <table name="sam_fa_indexes" comment_char="#">
        <columns>line_type, value, path</columns>
        <file path="/opt/galaxy/agent-data/sam_fa_indices.loc" />
    </table>
    <!-- Location of Picard dict file and other files -->
    <table name="picard_indexes" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/picard_index.loc" />
    </table>
    <!-- Location of SRMA dict file and other files -->
    <table name="srma_indexes" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/picard_index.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie mapper format for TopHat to use -->
    <table name="tophat_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bowtie_indices.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie color-space mapper format for TopHat to use -->
    <table name="tophat_indexes_color" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bowtie_indices_color.loc" />
    </table>
    <!-- Locations of indexes in the Bowtie2 mapper format for TopHat2 to use -->
    <table name="tophat2_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/bowtie2_indices.loc" />
    </table>
    <!-- Locations of configurations in the CCAT peak/region caller format -->
    <table name="ccat_configurations" comment_char="#">
        <columns>value, name, path</columns>
        <file path="/opt/galaxy/agent-data/ccat_configurations.loc" />
    </table>
    <!-- Location of Mosaik files -->
    <table name="mosaik_indexes" comment_char="#" allow_duplicate_entries="False">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/mosaik_index.loc" />
    </table>
    <!-- Locations of indexes in the 2bit format -->
    <table name="twobit" comment_char="#" allow_duplicate_entries="False">
        <columns>value, path</columns>
        <file path="/opt/galaxy/agent-data/twobit.loc" />
    </table>
    <!-- Location genome fasta files for cufflinks bias correction -->
    <table name="fasta_indexes" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/fasta_indexes.loc" />
    </table>
    <!-- Available IGV builds, loaded from URL -->
    <table name="igv_broad_genomes" comment_char="#" allow_duplicate_entries="False">
        <columns>name, url, value</columns>
        <file url="http://igv.broadinstitute.org/genomes/genomes.txt" />
    </table>
    <!-- Available liftOver chain file -->
    <table name="liftOver" comment_char="#" allow_duplicate_entries="False">
        <columns>dbkey, name, value</columns>
        <file path="/opt/galaxy/agent-data/liftOver.loc" />
    </table>
    <!-- Locations of indexes in the STAR mapper format -->
    <table name="rnastar_index" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/rnastar_index.loc" />
    </table>

    <!-- Location of all GFF/GTF files -->
    <table name="gene_sets" comment_char="#">
        <columns>value, dbkey, name, path</columns>
        <file path="/opt/galaxy/agent-data/gene_sets.loc" />
    </table>
    <!-- Location of featureCounts built-in annotation keys-->
    <table name="featurecounts_anno" comment_char="#">
        <columns>value, dbkey</columns>
        <file path="/opt/galaxy/agent-data/featurecounts_anno.loc" />
    </table>
    <!-- iobio bam servers -->
    <table name="bam_iobio" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, url</columns>
        <file path="/opt/galaxy/agent-data/bam_iobio.loc" />
    </table>
    <!-- iobio vcf servers -->
    <table name="vcf_iobio" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, url</columns>
        <file path="/opt/galaxy/agent-data/vcf_iobio.loc" />
    </table>
    <!-- simple biom servers -->
    <table name="biom_simple_display" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, url</columns>
        <file path="/opt/galaxy/agent-data/biom_simple_display.loc" />
    </table>
    <!-- simple intermine servers -->
    <table name="intermine_simple_display" comment_char="#" allow_duplicate_entries="False">
        <columns>value, name, url</columns>
        <file path="/opt/galaxy/agent-data/intermine_simple_display.loc" />
    </table>
</tables>
